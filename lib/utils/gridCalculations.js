import{GridOrientation,GridStrategy,GridScrollDirection}from"../types/grid";export function calculateGridPosition(index,dimensions,orientation){"worklet";const{columns=3,rows=3,itemWidth,itemHeight,rowGap=0,columnGap=0}=dimensions;let row;let column;if(orientation===GridOrientation.Vertical){row=Math.floor(index/columns);column=index%columns}else{column=Math.floor(index/rows);row=index%rows}const x=column*(itemWidth+columnGap);const y=row*(itemHeight+rowGap);return{index,row,column,x,y}}export function calculateIndexFromRowColumn(row,column,dimensions,orientation){"worklet";const{columns=3,rows=3}=dimensions;if(orientation===GridOrientation.Vertical){return row*columns+column}else{return column*rows+row}}export function listToGridObject(list,dimensions,orientation){const positions={};for(let i=0;i<list.length;i++){const position=calculateGridPosition(i,dimensions,orientation);positions[list[i].id]=position}return positions}export function clamp(value,min,max){"worklet";return Math.max(min,Math.min(value,max))}export function getGridCellFromCoordinates(x,y,dimensions,orientation,totalItems){"worklet";const{columns=3,rows=3,itemWidth,itemHeight,rowGap=0,columnGap=0}=dimensions;const column=Math.floor(x/(itemWidth+columnGap));const row=Math.floor(y/(itemHeight+rowGap));const index=calculateIndexFromRowColumn(row,column,dimensions,orientation);const clampedIndex=clamp(index,0,totalItems-1);const clampedPosition=calculateGridPosition(clampedIndex,dimensions,orientation);return{row:clampedPosition.row,column:clampedPosition.column,index:clampedIndex}}export function reorderGridInsert(positions,activeId,targetIndex,dimensions,orientation){"worklet";const newPositions={};const activePosition=positions[activeId];const fromIndex=activePosition.index;if(fromIndex===targetIndex){return positions}const movingUp=targetIndex<fromIndex;for(const id in positions){const currentIndex=positions[id].index;if(id===activeId){newPositions[id]=calculateGridPosition(targetIndex,dimensions,orientation)}else if(movingUp&&currentIndex>=targetIndex&&currentIndex<fromIndex){newPositions[id]=calculateGridPosition(currentIndex+1,dimensions,orientation)}else if(!movingUp&&currentIndex<=targetIndex&&currentIndex>fromIndex){newPositions[id]=calculateGridPosition(currentIndex-1,dimensions,orientation)}else{newPositions[id]=positions[id]}}return newPositions}export function reorderGridSwap(positions,activeId,targetId,dimensions,orientation){"worklet";const newPositions={...positions};const activePosition=positions[activeId];const targetPosition=positions[targetId];newPositions[activeId]=targetPosition;newPositions[targetId]=activePosition;return newPositions}export function setGridPosition(x,y,scrollX,scrollY,itemsCount,positions,id,dimensions,orientation,strategy){"worklet";const adjustedX=x+scrollX;const adjustedY=y+scrollY;const targetCell=getGridCellFromCoordinates(adjustedX,adjustedY,dimensions,orientation,itemsCount);const currentIndex=positions.value[id].index;if(targetCell.index===currentIndex){return}let targetId=null;for(const itemId in positions.value){if(positions.value[itemId].index===targetCell.index){targetId=itemId;break}}if(strategy===GridStrategy.Insert){positions.value=reorderGridInsert(positions.value,id,targetCell.index,dimensions,orientation)}else if(strategy===GridStrategy.Swap&&targetId){positions.value=reorderGridSwap(positions.value,id,targetId,dimensions,orientation)}}export function calculateGridContentDimensions(itemsCount,dimensions,orientation){"worklet";const{columns=3,rows=3,itemWidth,itemHeight,rowGap=0,columnGap=0}=dimensions;if(orientation===GridOrientation.Vertical){const totalRows=Math.ceil(itemsCount/columns);const width=columns*itemWidth+(columns-1)*columnGap;const height=totalRows*itemHeight+(totalRows-1)*rowGap;return{width,height}}else{const totalColumns=Math.ceil(itemsCount/rows);const width=totalColumns*itemWidth+(totalColumns-1)*columnGap;const height=rows*itemHeight+(rows-1)*rowGap;return{width,height}}}export function setGridAutoScroll(x,y,scrollX,scrollY,containerWidth,containerHeight,scrollThreshold,autoScrollDirection){"worklet";const leftBound=scrollX;const rightBound=scrollX+containerWidth;const topBound=scrollY;const bottomBound=scrollY+containerHeight;const isNearLeft=x<leftBound+scrollThreshold;const isNearRight=x>rightBound-scrollThreshold;const isNearTop=y<topBound+scrollThreshold;const isNearBottom=y>bottomBound-scrollThreshold;if(isNearTop&&isNearLeft){autoScrollDirection.value=GridScrollDirection.UpLeft}else if(isNearTop&&isNearRight){autoScrollDirection.value=GridScrollDirection.UpRight}else if(isNearBottom&&isNearLeft){autoScrollDirection.value=GridScrollDirection.DownLeft}else if(isNearBottom&&isNearRight){autoScrollDirection.value=GridScrollDirection.DownRight}else if(isNearTop){autoScrollDirection.value=GridScrollDirection.Up}else if(isNearBottom){autoScrollDirection.value=GridScrollDirection.Down}else if(isNearLeft){autoScrollDirection.value=GridScrollDirection.Left}else if(isNearRight){autoScrollDirection.value=GridScrollDirection.Right}else{autoScrollDirection.value=GridScrollDirection.None}}export function findItemIdAtIndex(positions,index){"worklet";for(const id in positions){if(positions[id].index===index){return id}}return null}